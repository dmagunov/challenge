{
  "name": "@design-system/design-tokens",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./dist/*"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist ",
    "clean:build": "rm -rf dist",
    "build": "pnpm run clean:build && pnpm run build:shadcn",
    "build:shadcn": "cobalt build --config ./transforms/shadcn.config.mjs"
  },
  "devDependencies": {
    "@cobalt-ui/cli": "^1.11.3",
    "@cobalt-ui/plugin-css": "^1.7.5",
    "@cobalt-ui/plugin-js": "^1.4.4",
    "@cobalt-ui/plugin-tailwind": "^0.0.3",
    "culori": "^4.0.1"
  }
}
